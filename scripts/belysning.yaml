  varde_ljus:
    sequence:
      - service: light.turn_on
        alias: Fullt ljus
        data:
          entity_id: light.nexadimmer_level
          brightness: 255

  mysbelysning:
    sequence:
      - service: light.turn_on
        alias: Softish
        data:
          entity_id: light.nexadimmer_level
          brightness: 40

  godnatt:
    sequence:
      - service: light.turn_off
        alias: Släcker
        data:
          entity_id: light.nexadimmer_level